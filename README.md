# SQL SELECT & WHERE Clause Practice

This repository contains structured SQL practice queries focused on the **SELECT** and **WHERE** clauses.  
It is designed to build strong fundamentals required for **Data Analyst** and **SQL interview preparation**.

---

## 📌 Contents

- `select_queries.sql`  
  Practice queries covering:
  - Selecting all and specific columns
  - Column aliasing
  - DISTINCT values
  - Calculated columns
  - ORDER BY and LIMIT usage

- `where_clause_queries.sql`  
  Practice queries covering:
  - Filtering using comparison operators
  - Logical operators (`AND`, `OR`, `NOT`)
  - `BETWEEN`, `IN`, `LIKE`
  - `IS NULL` / `IS NOT NULL`
  - Subqueries inside `WHERE`

---

## 🧠 Key Concepts Practiced

- Row-level filtering using `WHERE`
- Correct handling of `NULL` values
- Pattern matching with `LIKE`
- Understanding operator precedence
- Using subqueries for conditional filtering
- Writing readable and interview-safe SQL queries

---

## 🎯 Purpose

This repository is part of a structured SQL learning path to:
- Strengthen core SQL fundamentals
- Avoid common beginner mistakes
- Practice queries commonly asked in interviews
- Build a clean and honest SQL portfolio

---

## 🛠️ Database Compatibility

Queries are written to be compatible with:
- MySQL  
- PostgreSQL  

Minor syntax changes may be required for other SQL engines.

---

## 📈 Next Steps

Future extensions may include:
- `GROUP BY` and `HAVING`
- `JOIN` queries
- Advanced subqueries

---

**Author:** Aspiring Data Analyst - Sarah Naqvi 
**Focus:** Clean SQL fundamentals and consistent practice
